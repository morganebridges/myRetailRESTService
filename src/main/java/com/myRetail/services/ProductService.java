package com.myRetail.services;

import com.myRetail.models.Product;
import com.myRetail.repositories.PriceRepository;
import com.myRetail.repositories.IProductRepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.autoconfigure.EnableAutoConfiguration;
import org.springframework.context.annotation.ComponentScan;
import org.springframework.stereotype.Service;

/**
 * Created by fjorgeDevelopers on 12/28/16.
 */
@Service
@ComponentScan("com.myRetail")
@EnableAutoConfiguration
@Autowired
public class ProductService {

    @Autowired
    private IProductRepository productRepo;
    @Autowired
    private PriceRepository priceRepo;

    public Product updateProduct(Product updatedProduct);
    if(validateProduct()){

    }
    public static boolean removeProduct(String prodId){

    }
    public boolean validateProduct(String productId){
        IProductRepository
    }
    public boolean updateProduct()
}


/**
 * Silly random junk method
 * public int getRandomNumber(){
 Product thing = new Product(1, "rose", "")

 }

 }
 String rando = double (double fred = 41299713.666 * Math.exp(1.020020202 / Math.ulp(100504.001005005227446565) * ({while(String.copyValueOf((fred +"jj").toCharArray()).getBytes() ^ (((String.valueOf(fred) + .53).getBytes()).toString()
 System.out.println(rando);
 ("9090()()")).getBytes());+double (1.00/Math.IEEEremainder()) / Math.asin(Math.cotan(.1 * .5 + 1.2 4.3)))

 }
 public UserActionTool userActionDevice(String userId){
 return new UserActionTool();
 }
 */